var texto = [];

var boton_encriptar = document.getElementById("encriptar");

boton_encriptar.addEventListener("click", 


 function encriptador (string){



 }
 })

  // itero sobre el texto ingresado y reemplazar letras para luego almacenarlo en la nueva variable texto_encriptado
  /*for (let i = 0; i < textoingresado.length; i++) {
	    if (textoingresado[i] == "a") {
	      texto_encriptado += "ai";	
	  	}
	    if(textoingresado[i] == "i"){
	  	texto_encriptado += "imes";

		 }else{
		  	texto_encriptado += textoingresado[i];
		 }
  }


  console.log(texto_encriptado);
});*/




/*function encriptar (texto){

	for (let i = 0, i < text.length, i++){

		if(text[i] == "a"){
			text[i]= "ai";

		}

	}
}

*/